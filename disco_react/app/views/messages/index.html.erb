<%= event_source('MessageProjection') %>

<%= react_component('MessageList', {
    messages: @messages,
    create_path: messages_path,
    csrf_param: request_forgery_protection_token,
    csrf_token: form_authenticity_token
}, prerender: true) %>
